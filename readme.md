```
# install everything
npm run i:all

# if it's not working see below

# launch servers
npm run dev

# don't mind the babel warnings

# go to http://localhost:3000/
# donate
# feel good
```

```
# in case npm i:all is not working (you're on windows)

npm i && cd packages/raiseup-app && npm i && cd ../raiseup-server && npm i && cd ../../
```
